# Exercise
Study `src/main/java/com/learning/java/orders/api/OrderController.java`, then replace its private map with an injected service. Add a `MockMvc` test for create, get, list, and status change before comparing your result to the provided reference controller.
